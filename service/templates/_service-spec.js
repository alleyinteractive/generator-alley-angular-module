describe('<%= modulePrefix %><%= _capitalize(_.camelize(moduleNamePart)) %>', function() {

  beforeEach(module('<%= githubName %>.<%= moduleName %>'));

  it('should ...', inject(function('<%= modulePrefix %><%= _.capitalize(moduleNamePart)) %>'))) {

    /* 
    To test your service, test against the methods you've created
    expect(myService.one).toEqual(1);
    */

  };
});
